# docs/diagrams/

Diagrams generated by `skills/archify/SKILL.md` — self-contained HTML (dark/light, export PNG/SVG).

- `.agent/graph.md` → `layer-plan.html` (dependency layers + human checkpoints)
- `.agent/design.md` → `architecture` + `workflow` diagrams

Source JSON candidates live in `.context/arch/`; the rendered HTML lives here.
